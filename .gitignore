# Python
__pycache__/
*.pyc
.venv/
venv/
.env
.pytest_cache/
.mypy_cache/
.ruff_cache/
*.egg-info/

# OS
.DS_Store

# Keep raw JSON dumps and the SQLite DB out of git (regenerate with main.py).
data/raw/*.json
data/f1.db

# Run summaries are regenerated each pipeline run
logs/run_summary.json

# Power BI
*.pbix.bak
